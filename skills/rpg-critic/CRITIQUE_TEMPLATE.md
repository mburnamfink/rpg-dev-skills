# {Module Title} — Critique & Response Log

*Critical read of [`{module-file}.md`](./{module-file}.md) by Claude, {YYYY-MM-DD}. Checked against: {reference files — setting/lore, campaign notes, system rules — or "none provided"}. Not a gloss — strengths are noted only where load-bearing to a finding. Each finding has a stable ID so responses can reference it. Fill the `→ Response:` line under any item; mark **status** as `accepted` / `rejected` / `deferred` / `discuss`.*

**How to read this:** each finding is a GitHub admonition block; your reply goes on the `→ Response:` line beneath it. The block type is the severity — 🔴 `[!CAUTION]` will break at the table · 🟠 `[!WARNING]` design/pacing weakness · 🟡 `[!IMPORTANT]` unanswered question or canon/rules clash · 🔵 `[!TIP]` enhancement.

---

## Z. Notes from the author to the LLM
> A problem noted by the human for the LLM to resolve. Opposite pattern from the rest of the template. Leave present but blank.

## A. Will break mid-session (fix before running)

### A1 🔴 — {one-line diagnosis}

> [!CAUTION]
> {The problem, specific and cited to a scene/section. Then the concrete fix — a number, a branch, a cut — often drawn from the module's own material.}

→ Response:
→ Status:

<!-- Repeat A2, A3… Reserve section A for 🔴 contradictions and undefined load-bearing rules. -->

---

## B. Off-the-rails scenarios not yet armored against

### B1 🔴 — {the genre-savvy / chaotic move the module doesn't handle}

> [!CAUTION]
> {What the table does, why the module can't absorb it, and the fallback to add.}

→ Response:
→ Status:

---

## C. NPC motivation cracks

### C1 🟠 — {the motive that doesn't hold}

> [!WARNING]
> {The contradiction or unplayable performance; the reconciliation, ideally already latent in the text.}

→ Response:
→ Status:

---

## D. Pacing and the boring-risk

### D1 🟠 — {the dead zone or monotony}

> [!WARNING]
> {Where the game sags; the concrete restructure — a spine, a clock, a branch, a compression under time pressure.}

→ Response:
→ Status:

---

## E. Questions players will ask that have no answer

### E1 🟡 — "{the question, in the players' voice}"

> [!IMPORTANT]
> {Why it comes up in the first ten minutes; what to pre-stub so it's pick-up-and-play.}

→ Response:
→ Status:

---

## F. Canon & rules consistency

<!-- Only if reference files were provided. Cite the reference file + section, not just the module. -->

### F1 🟡 — {contradiction with setting / campaign / system rules}

> [!IMPORTANT]
> {What the module asserts, what canon says (quote/cite the reference file), and how to reconcile — usually bend the module, not the setting.}

→ Response:
→ Status:

---

## G. Descriptive details that would earn their keep

### G1 🔵 — {the enrichment}

> [!TIP]
> {A sensory signature / motif / physical tell that makes the situation land. Clearly optional polish.}

→ Response:
→ Status:

---

## Triage — what I'd fix first

If only five before running: **{IDs}**. Everything in G is optional polish.

*{Note any findings that may be deliberate choices, not bugs — flag them `rejected` with a note if so.}*
